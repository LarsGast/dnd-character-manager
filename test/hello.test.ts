import { describe, expect, it } from 'vitest';

// A simple test to verify that the testing setup is working.
// Needed because there are currently no other tests.
describe('Dummy Tests', () => {
	it('should pass', () => {
		expect(true).toBe(true);
	});
});
