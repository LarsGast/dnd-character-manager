export interface ResourceReferenceApiDto {
	/**
	 * Unique identifier in the 5e SRD API.
	 */
	index: string;

	/**
	 * Display name of the object.
	 */
	name: string;

	/**
	 * Url to the object within the API.
	 */
	url: string;
}
