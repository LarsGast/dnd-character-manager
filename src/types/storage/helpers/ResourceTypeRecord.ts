import type { ResourceType } from '../../domain/helpers/ResourceType';

/**
 * Enum for resource types for communication with the database.
 * @see {@link ResourceType}
 */
export enum ResourceTypeRecord {
	Race,
	Subclass,
}
