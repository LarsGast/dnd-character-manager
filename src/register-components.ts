/**
 * Main component registration module.
 * Imports and registers all custom HTML components used throughout the application.
 * This ensures all custom elements are available when the DOM is ready.
 */
import './features/character/index';
import './features/homebrew/index';
